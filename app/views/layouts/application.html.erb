<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="wrapper">
        <nav class="top-bar top" data-topbar role="navigation">
          <ul class="title-area">
            <li class="name">
              <h1><a href="/">Rails Template</a></h1>
            </li>
          </ul>

          <section class="top-bar-section">
            <ul class="right">
              <li class="<%= yield(:homeActive) %>"><a href="/">Home</a></li>
              <li class="<%= yield(:aboutActive) %>"><a href="/about">About</a></li>
              <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
            </ul>

          </section>
        </nav>
        <div class="content">
          <%= yield %>
        </div>
        <footer>
          <div class="row">
            <div class="small-12 columns"><p>Footer Content</p></div>
          </div>
        </footer>
  
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>